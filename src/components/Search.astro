<section class="site-search" aria-labelledby="site-search-label" data-site-search>
	<p class="site-search__label" id="site-search-label">Search notes</p>
	<div class="site-search__fallback">
		<svg aria-hidden="true" viewBox="0 0 24 24" width="18" height="18">
			<circle cx="11" cy="11" r="6.5" fill="none" stroke="currentColor" stroke-width="1.75"></circle>
			<path d="m16 16 4 4" fill="none" stroke="currentColor" stroke-linecap="round" stroke-width="1.75"></path>
		</svg>
		<input type="search" aria-label="Search notes" placeholder="Search available after build" disabled />
	</div>
	<pagefind-config excerpt-length="18"></pagefind-config>
	<pagefind-searchbox placeholder="Search the notebook…" max-results="6" show-sub-results shortcut="/"
	></pagefind-searchbox>
	<noscript><p class="site-search__noscript">Search requires JavaScript.</p></noscript>
</section>

<script>
	const search = document.querySelector("[data-site-search]");

	customElements.whenDefined("pagefind-searchbox").then(() => {
		search?.classList.add("site-search--ready");
	});
</script>

<style>
	.site-search {
		--pf-text: var(--color-text);
		--pf-text-secondary: var(--color-muted);
		--pf-text-muted: var(--color-muted);
		--pf-background: var(--color-surface);
		--pf-border: var(--color-border);
		--pf-border-focus: var(--color-accent);
		--pf-skeleton: var(--color-surface-raised);
		--pf-skeleton-shine: var(--color-border);
		--pf-hover: var(--color-selection);
		--pf-mark: var(--color-text);
		--pf-outline-focus: var(--color-accent);
		--pf-font: var(--font-sans);
		--pf-input-height: 2.5rem;
		--pf-input-font-size: 1rem;
		--pf-result-title-font-size: var(--text-sm);
		--pf-result-excerpt-font-size: var(--text-xs);
		--pf-border-radius: var(--radius-md);
		--pf-searchbox-max-width: 100%;
		--pf-searchbox-dropdown-max-height: min(24rem, 55vh);
		--pf-shadow-lg: 0 1rem 3rem var(--color-shadow);

		position: relative;
		z-index: 2;
		padding: var(--space-4);
		border-top: 1px solid var(--color-border);
		font-family: var(--font-sans);
	}

	.site-search__label {
		margin-bottom: var(--space-2);
		color: var(--color-muted);
		font-size: 0.64rem;
		font-weight: 500;
		letter-spacing: var(--tracking-caps);
		line-height: 1.2;
		text-transform: uppercase;
	}

	.site-search__fallback {
		display: flex;
		align-items: center;
		gap: var(--space-2);
		height: 2.5rem;
		padding-inline: var(--space-3);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-md);
		background: var(--color-surface);
		color: var(--color-muted);
		opacity: 0.72;
	}

	.site-search__fallback svg {
		flex: 0 0 auto;
	}

	.site-search__fallback input {
		width: 100%;
		min-width: 0;
		border: 0;
		background: transparent;
		color: var(--color-muted);
		font-size: 1rem;
		outline: 0;
	}

	.site-search__fallback input:disabled {
		cursor: not-allowed;
	}

	.site-search__fallback input::placeholder {
		color: var(--color-muted);
		opacity: 1;
	}

	.site-search pagefind-searchbox {
		display: none;
	}

	.site-search--ready .site-search__fallback {
		display: none;
	}

	.site-search--ready pagefind-searchbox {
		display: block;
	}

	.site-search__noscript {
		margin-top: var(--space-2);
		color: var(--color-muted);
		font-size: var(--text-xs);
	}
</style>
