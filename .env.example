# Create this in Bluesky under Settings > Privacy and security > App passwords.
ATPROTO_APP_PASSWORD=

# Optional overrides. The sync normally uses the configured handle and discovers its PDS.
# ATPROTO_HANDLE=desertthunder.dev
# ATPROTO_SERVICE=https://example-pds.invalid
